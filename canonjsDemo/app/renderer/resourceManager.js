
'use strict'

define([], function()
{
    // kept simple
    // this is just acting as a common object between elements
    // elements so far => cars wheel & car chassis

    var rmanager = {};

    return rmanager;
});
