
#pragma once

#include <cmath>

namespace math
{
  constexpr float pi = float(M_PI);
  constexpr float pi2 = pi * 2.0f;
  constexpr float hpi = pi * 0.5f; // h = half
  constexpr float qpi = pi * 0.25f; // q = quart
  constexpr float opi = pi * 0.125f; // o = octo
}

